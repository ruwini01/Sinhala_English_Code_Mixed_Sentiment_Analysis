# While true (or unset), the app uses the built-in mock predictor.
# Set to "false" to call real backend instead.
NEXT_PUBLIC_USE_MOCK=true

# Model backend address (only used when USE_MOCK=false).
NEXT_PUBLIC_API_URL=http://localhost:8000
